#include "Font.h"
/*
#include <stdexcept>
bool VectorFont::AddCharacterUTF8(uint32_t code_point, GlyphData data){
	this->glyphs.insert(std::pair<uint32_t, GlyphData>(code_point, data));
}
bool VectorFont::GetRGBA32RenderedGlyphFromUTF8(uint32_t code_point, float pixels_per_em, uint32_t* target, float* offsets){
	GlyphData* data = 0;
	try {
		data = &this->glyphs.at(code_point);
	} catch (std::out_of_range e) {
		return false;
	}
}*/
